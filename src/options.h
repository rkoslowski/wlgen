#pragma once

#include <stdio.h>

class Options{
public:
    
    Options();
    ~Options();
    
    // here parsing and assigning functions
    
private:
    //here all options and parameter
    
}
